#!/usr/bin/env bash
set -e

echo "Running pre-commit checks..."

# Lint
echo "→ eslint"
npx eslint src/ index.js functions.js __tests__/ --quiet

# Tests
echo "→ jest"
npx jest --config jest.config.cjs --bail --silent

echo "✅ Pre-commit checks passed"
