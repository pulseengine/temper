load("@aspect_rules_jest//jest:defs.bzl", "jest_test")

jest_test(
    name = "unit_tests",
    node_modules = "//:node_modules",
    config = "//:jest.config.js",
    data = ["//src:lib"] + glob(["unit/**/*.test.js", "fixtures/**"]),
)

jest_test(
    name = "integration_tests",
    node_modules = "//:node_modules",
    config = "//:jest.config.js",
    data = ["//src:lib"] + glob(["integration/**/*.test.js", "fixtures/**"]),
)
